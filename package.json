{
    "name": "TestAutomation",
    "scripts": {
        "test": "npm install && npx cypress run"
    },
    "dependencies": {
        "cypress": "^9.7.0",
        "cypress-mochawesome-reporter": "^3.1.0"
    }
}
